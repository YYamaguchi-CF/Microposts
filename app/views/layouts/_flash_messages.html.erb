<% flash.each do |message_type, message| %>
	<div class="alert alert-<%= message_type %>">
		<% if message.is_a?(Array) %>
			<% message.each do |msg| %>
				<ul>
					<li><%= msg %></li>
				</ul>
			<% end %>
		<% else %>
			<%= message %>
		<% end %>
	</div>
<% end %>